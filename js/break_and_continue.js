(function (){
    "use strict";

    // var input = parseFloat(prompt("Enter an odd number between 1 and 50."));
    // var userNumber = (input % 2 !== 0);
    // for (var i = 1; i <= 50; i++) {
    //     if (userNumber !== i) {
    //         prompt("Enter an odd number between 1 and 50.");
    //     }
    // }

})();